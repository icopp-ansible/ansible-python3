---
galaxy_info:
  author: Ian Copp
  description: Install Python 3 via Homebrew. Does nothing on non-macOS platforms.
  license: MIT
  min_ansible_version: 1.2
  platforms:
    - name: MacOSX
      versions:
        - all
  galaxy_tags:
    - system

dependencies:
  - role: icopp.homebrew
    when: ansible_os_family == "Darwin"
